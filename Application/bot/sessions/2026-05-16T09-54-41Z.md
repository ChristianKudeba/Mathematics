---
parent-thread: cubic-moment
phase-1-option: 2
bias-profile-tag: LONG-HORIZON-CHIP-AWAY
concrete-progress: yes
---

# Session 2026-05-16 09:54 UTC

## Decision

Phase 1 option 2 (chip-away on long-horizon project `cubic-moment`, chunk II.3.b — second sub-chunk of II.3). Previous session (`2026-05-16T06-55-36Z`, session 10) closed II.3.a (Bianchi-Waldspurger framework + statement) and named II.3.b as the next chunk. II.3.b is "explicit per-place computation of $c_{\chi,\mathfrak{p}}$ at unramified $\mathfrak{p}\nmid q\mathfrak{p}_2$" — the unramified-Euler-factor case. This is the 11th consecutive cubic-moment session with concrete progress; well within bias profile (PROTOCOL.md §1 / §3 endorse 60+ session sustained chip-away). Th-chokepoint thread still awaiting Anton's intervention on Route (c) for $\widehat\Psi(1/2) = 0$ redesign.

## Replies ingested

None. Inbox check via `bot/check_inbox.py` failed with HTTP 400 / Bad Request (20th consecutive session). Verbatim error: `{"status": "error", "error": "HTTP Error 400: Bad Request", "type": "HTTPError"}`. Anton: re-run `npx @gongrzhe/server-gmail-autoauth-mcp auth` and update `GMAIL_REFRESH_TOKEN` per PROTOCOL.md §9.

## What I did

1. **Phase 0 read.** PROTOCOL.md, STRATEGY.md, last session `2026-05-16T06-55-36Z.md`, `bot/longterm/cubic-moment.md`, and the structural anchors of P17 (`grep -n "^### "`). Did NOT pre-read P1–P16 or REFEREE-REPORT.

2. **Wrote new subsection** "Local factor $c_{\chi,\mathfrak{p}}$ at unramified $\mathfrak{p}$ (II.3.b — explicit Euler factors)" in P17, between II.3.a Done-criterion and §Open sub-questions.

3. **Setup recap.** Recalled (II.3.a.9): $c_{\chi,\mathfrak{p}} = \prod_{w\mid\mathfrak{p}}L_w(1,\chi^2)\cdot L_\mathfrak{p}(1,\chi_{E/F})\cdot J_\mathfrak{p}(\pi_\chi)$. Locked $J_\mathfrak{p}(\pi_\chi) = 1$ at unramified $\mathfrak{p}$ (II.3.b.1) — spherical newvector. Cross-referenced to II.1.d (P1)–(P3) test function. Stated generic hypothesis $\chi^2 \neq \mathbf{1}$ (II.3.b CORE-8 fix).

4. **Split case (II.3.b.i).** Defined split Satake parameter $\alpha_\mathfrak{p} = \chi(\varpi_{w_1})$. Derived (II.3.b.2): Type-I + trivial $\chi_{E/F,\mathfrak{p}}$ at split unramified forces $\chi(\varpi_{w_2}) = \alpha_\mathfrak{p}^{-1}$. Wrote local $L$-factors and boxed closed form (II.3.b.3):
$$c_{\chi,\mathfrak{p}}^{\mathrm{split}} = (1 - \alpha_\mathfrak{p}^2 q_\mathfrak{p}^{-1})^{-1}(1 - \alpha_\mathfrak{p}^{-2} q_\mathfrak{p}^{-1})^{-1}(1 - q_\mathfrak{p}^{-1})^{-1}.$$

5. **Inert case (II.3.b.ii).** Local picture: $E_w/F_\mathfrak{p}$ unramified quadratic, $\varpi_w = \varpi_\mathfrak{p}$, $q_w = q_\mathfrak{p}^2$. Type-I + $\chi_{E/F,\mathfrak{p}}(\varpi_\mathfrak{p}) = -1$ (unramified order-$2$ character at inert) forces (II.3.b.4): **$\chi_w(\varpi_\mathfrak{p}) = -1$** — the inert-prime Satake parameter is fixed at $-1$ for *every* $\chi$ in the family. Boxed closed form (II.3.b.5):
$$c_{\chi,\mathfrak{p}}^{\mathrm{inert}} = (1 - q_\mathfrak{p}^{-2})^{-1}(1 + q_\mathfrak{p}^{-1})^{-1} = (1 - q_\mathfrak{p}^{-1})^{-1}(1 + q_\mathfrak{p}^{-1})^{-2}.$$

6. **Unification (II.3.b.iii).** Derived local Satake parameters of $\pi_\chi$: $(\alpha_\mathfrak{p}, \alpha_\mathfrak{p}^{-1})$ at split, $(i, -i)$ at inert (the latter via $\mu_i(\varpi_\mathfrak{p})^2 = \chi_w(\varpi_w) = -1$ — local rep structure, not formal $L$-factor factorization). Computed adjoint Satake parameters $(\alpha_\mathfrak{p}^2, 1, \alpha_\mathfrak{p}^{-2})$ at split and $(-1, 1, -1)$ at inert. Matched explicitly against (II.3.b.3) / (II.3.b.5):
$$\boxed{c_{\chi,\mathfrak{p}} = L_\mathfrak{p}(1, \mathrm{Ad}\,\pi_\chi)\qquad\text{at every unramified }\mathfrak{p}\nmid q\mathfrak{p}_2.}\tag{II.3.b.6}$$
**Numerical sanity check:** Python script with $\alpha = e^{0.7i}, q_\mathfrak{p} = 5$ — both split and inert formulas match $L_\mathfrak{p}(1, \mathrm{Ad})$ to machine precision (real parts agree, imaginary parts $\sim 10^{-17}$).

7. **Global $\chi$-aspect bound (II.3.b.iv).** Via $L_\mathfrak{p}(1, \mathrm{Ad}\,\pi_\chi) = 1$ at ramified $\mathfrak{p}\mid q$ (under generic non-quadratic-on-residues condition — CORE-6 caveat), got (II.3.b.8):
$$c_\chi^{\mathrm{ur}} = L(1, \mathrm{Ad}\,\pi_\chi)/(L_{\mathfrak{p}_2}(1,\mathrm{Ad}\,\pi_\chi)\cdot L_\infty(1,\mathrm{Ad}\,\pi_\chi)).$$
Effective upper bound (II.3.b.9): $c_\chi^{\mathrm{ur}} \ll (\log|q|)^{O(1)} = |q|^{o(1)}$ via convexity + Mertens for $L_E(1, \chi^2)$. **Lower bound flagged as Siegel-ineffective** in the dihedral CM case (only upper needed for Phase IV).

8. **Six remarks (R1)–(R6).** (R1) (II.3.b.6) is the "clean form" promised in II.3.a; (R2) inert Satake fixed because the local rep $\pi_{\chi,\mathfrak{p}}$ is dihedral-unramified intrinsic to $E/F$ — *plus existence remark* (CORE-4 fix) on Type-I solvability at all inert primes simultaneously, noting size $\asymp |q|$ for the conductor-$q$ family matches the cube-moment count; (R3) II.2.b/II.3.b consistency on the $\alpha^2$-doubling at split primes; (R4) FH 1995 over-$\mathbb{Q}$ analog comparison; (R5) conductor invariance of $J_\mathfrak{p}$ at unramified $\mathfrak{p}$; (R6) convergence of $c_\chi^{\mathrm{ur}}$ at $s = 1$ for $\chi^2 \neq \mathbf{1}$.

9. **Q6 updated to THRICE-PARTIALLY-RESOLVED** in P17 §Open sub-questions.

10. **P17 Session log row appended** for session 11.

11. **`bot/longterm/cubic-moment.md`** — Next chunk advanced from II.3.b to II.3.c. Session log row appended.

12. **`bot/STRATEGY.md`** — header + §3 current sub-goal + §4 chunks-completed (10 → 11) + §7 session-log row updated.

13. **Skeptic loop.** Round 1 raised 8 CORE (CORE-1/2/4/5/6/7/8 substantive + CORE-3 acknowledged OK). All 5 substantive CORE fixed in source (see below). Round 2 not run — fixes are derivation tightening + explicit caveats; skeptic Round 1 explicitly confirmed the boxed unified formula (II.3.b.6) and the closed-form local factors (II.3.b.3)+(II.3.b.5) as correct.

## Skeptic dialogue

**Round 1.** General-purpose subagent. Brief: scrutinize spherical $J_\mathfrak{p}=1$ citation, Type-I split derivation, Type-I inert derivation, inert local adjoint identity / Satake parameter justification, dihedral $L_\mathfrak{p}(s,\mathrm{Ad}) = 1$ condition, unconditional log-bound effectivity, $\chi^2 \neq 1$ convergence hypothesis.

**Skeptic Round 1 raised 8 CORE (categorized):**

- **CORE-1 (Inert Satake $(i,-i)$ derivation non-rigorous):** the formal factorization $(1+X^2) = (1-iX)(1+iX)$ derives a numerical answer but is not a valid derivation of Satake parameters. Need to derive from local rep structure $\pi_{\chi,\mathfrak{p}} = \mathrm{Ind}(\mu_1, \mu_2)$ with $\mu_i(\varpi)^2 = \chi_w(\varpi_w) = -1$. Conclusion correct, derivation wrong.
- **CORE-2 (Irreducibility check at inert):** $(i, -i)$ Satake → $\mu_1\mu_2^{-1}$ unramified order-$2$ character, which is unitary, not $|\cdot|^{\pm 1}$ → irreducible tempered principal series. Should be stated.
- **CORE-3 (Type-I split derivation OK):** correct as written.
- **CORE-4 (Type-I forces inert constraint at every inert $\mathfrak{p}$ simultaneously):** is the resulting family non-empty? Should flag existence/density.
- **CORE-5 (Spherical $J_\mathfrak{p}=1$ needs cross-ref to II.1.d):** H–L 1994 Thm 0.2 is the global formula; the $J_v = 1$ assertion at unramified $v$ requires the test function = spherical newvector, which is the II.1.d (P1)–(P3) statement.
- **CORE-6 (Dihedral $L_\mathfrak{p}(s,\mathrm{Ad}) = 1$ generic condition):** requires $\chi^2|_{\mathcal{O}_{E_w}^\times}$ non-trivial — non-generic case where $\chi_w$ has order $2$ on residues makes $L_w(s,\chi^2)$ a non-trivial Euler factor (but bounded, so $|q|^{o(1)}$ conclusion unaffected).
- **CORE-7 (Unconditional log-bound effectivity):** $L(1, \mathrm{Ad}\,\pi)$ lower bound for dihedral CM forms reduces to Siegel-type ineffective bounds. Upper bound is fine; lower is technically ineffective.
- **CORE-8 (Convergence at $s=1$ requires $\chi^2 \neq \mathbf{1}$):** should be stated explicitly.

Plus **3 cosmetic:** (a) algebraic identity at line 1015 checked OK; (b) $|\varpi_w|_w$ notation OK; (c) "Galois-orbit" vs "conjugate-self-dual" mislabel.

**Skeptic conclusion:** "The boxed unified formula (II.3.b.6) is correct, but three derivations (CORE-1, CORE-6, CORE-7) need rework. Two implicit hypotheses (CORE-4, CORE-8) need to be stated."

**Round 1 author response (substantive CORE fixes applied to source).**

- **CORE-1 fix.** Rewrote inert Satake derivation: $\pi_{\chi,\mathfrak{p}} = \mathrm{Ind}_B^{GL_2}(\mu_1, \mu_2)$ with $\mu_i$ unramified, $\mu_i(\varpi_\mathfrak{p})^2 = \chi_w(\varpi_w) = -1$ giving $\mu_i = \pm i$. Local-$L$ consistency then matches as a derived check.
- **CORE-2 fix.** Added irreducibility check: $\mu_1\mu_2^{-1}(\varpi_\mathfrak{p}) = -1$, $|\mu_1\mu_2^{-1}| = 1$, so $\mu_1\mu_2^{-1}$ is unramified order-$2$ character, not $|\cdot|^{\pm 1}$ — irreducible tempered principal series.
- **CORE-4 fix.** Added existence remark in (R2): the conjugate-self-dual Hecke characters $\chi$ on $E^\times\backslash\mathbb{A}_E^\times$ of conductor $q$ with $\chi|_{F^\times} = \chi_{E/F}$ form a torsor of size $\asymp |q|$ (matching cube-moment family count); the constraint "$\chi_w(\varpi_\mathfrak{p}) = -1$ at every inert $\mathfrak{p}$" is automatically built into the global Type-I condition.
- **CORE-5 fix.** Replaced the bare H–L 1994 citation with explicit cross-reference to II.1.d (P1)–(P3): test function $\phi_\mathfrak{p}^{\mathrm{ur}} = \mathbf{1}_{\mathcal{O}_{E_\mathfrak{p}}}$ is $K^{\mathrm{ur}}$-spherical, factorizable, $\widehat\phi = \phi$, hence proportional to the spherical newvector matrix coefficient → Whittaker ratio = $1$ → $J_\mathfrak{p} = 1$. This is *definitional* in our setup, not a separate H–L assertion.
- **CORE-6 fix.** Replaced "the standard fact" with a generic-condition statement and caveat paragraph: $L_\mathfrak{p}(s, \mathrm{Ad}\,\pi_\chi) = 1$ at $\mathfrak{p}\mid q$ requires $\chi_w^2$ ramified at $w$, i.e., $\chi_w|_{\mathbb{F}_{q_F}^\times}$ has odd order. Non-generic quadratic-on-residues case makes $L_w(s,\chi^2)$ a non-trivial $O(1)$ Euler factor — modifies (II.3.b.8) by a bounded factor at offending $\mathfrak{p}\mid q$, *unchanging* the $|q|^{o(1)}$ conclusion of (II.3.b.9).
- **CORE-7 fix.** Split bound into upper/lower: upper $L(1, \mathrm{Ad}\,\pi_\chi) \ll (\log|q|)^{O(1)}$ unconditional via convexity + Mertens (Iwaniec–Kowalski §5.10); lower $\gg_\epsilon |q|^{-\epsilon}$ Siegel-ineffective in the dihedral CM case. Stated explicitly that only upper needed for Phase IV; lower deferred to IV.3 if load-bearing.
- **CORE-8 fix.** Added explicit "Generic hypothesis" paragraph stating $\chi^2 \neq \mathbf{1}$ globally as a standing assumption.

**Round 2.** Not run. Rationale: the fixes are derivation tightening + explicit caveats, the boxed closed forms (II.3.b.3), (II.3.b.5), (II.3.b.6) are unchanged and were explicitly confirmed correct by skeptic Round 1, and the global bound (II.3.b.9) survives the CORE-6/7 caveats unchanged.

## Result

**Chunk II.3.b completed with CONSENSUS-WITH-CAVEAT.** Concrete deliverables:

- New "Local factor $c_{\chi,\mathfrak{p}}$ at unramified $\mathfrak{p}$ (II.3.b — explicit Euler factors)" subsection in P17.
- **Explicit closed form** $c_{\chi,\mathfrak{p}}^{\mathrm{split}} = (1 - \alpha_\mathfrak{p}^2/q_\mathfrak{p})^{-1}(1 - \alpha_\mathfrak{p}^{-2}/q_\mathfrak{p})^{-1}(1 - 1/q_\mathfrak{p})^{-1}$ at split unramified $\mathfrak{p}$ (II.3.b.3).
- **Explicit closed form** $c_{\chi,\mathfrak{p}}^{\mathrm{inert}} = (1 - 1/q_\mathfrak{p}^2)^{-1}(1 + 1/q_\mathfrak{p})^{-1}$ at inert unramified $\mathfrak{p}$ (II.3.b.5), with the key observation (II.3.b.4) that the inert-prime Satake parameter is fixed at $-1$ for every $\chi$ in the family.
- **Unification (II.3.b.6):** $c_{\chi,\mathfrak{p}}^{\mathrm{ur}} = L_\mathfrak{p}(1, \mathrm{Ad}\,\pi_\chi)$ at every unramified $\mathfrak{p}\nmid q\mathfrak{p}_2$ — clean formula derived by matching against the auto-induced Satake parameters $(\alpha, \alpha^{-1})$ at split and $(i, -i)$ at inert. Numerically sanity-checked.
- **Global $\chi$-aspect bound (II.3.b.9):** $c_\chi^{\mathrm{ur}} \ll (\log|q|)^{O(1)} = |q|^{o(1)}$ unconditional upper bound. Structurally confirms all $|q|^{1+\epsilon}$ dominant size of $c_\chi$ comes from the ramified primes — substance of II.3.c.
- **Six remarks (R1)–(R6).**
- **Q6 updated to THRICE-PARTIALLY-RESOLVED.**

P17 grew from ~113 KB to ~125 KB.

## Consensus: WITH-CAVEAT

Round 1 raised 8 CORE issues; 5 substantive (CORE-1, CORE-2, CORE-4, CORE-5, CORE-6, CORE-7, CORE-8) fixed in source; CORE-3 acknowledged correct. Round 2 not run. Carry-forward caveats:

- **CORE-6 non-generic case:** the global identity (II.3.b.8) is modified by a bounded factor at primes $\mathfrak{p}\mid q$ where $\chi_w$ has order $2$ on residues. Does *not* affect the $|q|^{o(1)}$ conclusion of (II.3.b.9). Generic in the cube-moment family (most $\chi$ have order $> 2$ on residues).
- **CORE-7 lower bound effectivity:** the lower bound on $L(1, \mathrm{Ad}\,\pi_\chi)$ is Siegel-ineffective in the dihedral CM case. Only the upper bound is needed for Phase IV bookkeeping (the $|q|^{1+\epsilon}$ size of $c_\chi$ comes from the ramified primes, not the unramified bound).
- **Spherical $J_\mathfrak{p} = 1$:** the cross-reference to II.1.d (P1)–(P3) makes this definitional given our test-function choice. If a future revision of the test function at unramified $\mathfrak{p}$ is needed, $J_\mathfrak{p}$ must be re-computed.
- **(II.3.b) derivations assume $\chi^2 \neq \mathbf{1}$ globally** (the order-$2$ edge case is excluded — consistent with cube-moment input).

## Verdict: PROGRESS

Eleventh chunk of cubic-moment Phase II completed. Three concrete pieces of progress:

1. **Explicit closed forms for $c_{\chi,\mathfrak{p}}^{\mathrm{ur}}$ at unramified split and inert $\mathfrak{p}$** — (II.3.b.3) and (II.3.b.5). These are the explicit Euler factors that the H–L formula contributes to $c_\chi$ at unramified places, with the per-place factor depending on the split Satake parameter $\alpha_\mathfrak{p}$ at split primes and *fixed for all $\chi$* at inert primes.
2. **Unified clean formula $c_{\chi,\mathfrak{p}}^{\mathrm{ur}} = L_\mathfrak{p}(1, \mathrm{Ad}\,\pi_\chi)$** — (II.3.b.6) — a structurally satisfying identification that ties the per-place Bianchi-Waldspurger factor at unramified $\mathfrak{p}$ to a *single intrinsic* invariant of $\pi_\chi$ at $\mathfrak{p}$. The derivation requires local Satake-parameter computation for $\pi_\chi$, which uses (a) Arthur–Clozel auto-induction structure and (b) Type-I global condition; these inputs were *already locked* in II.1.d.bis / II.3.a, so II.3.b is an immediate consequence given the framework.
3. **Global $\chi$-aspect bound (II.3.b.9):** $c_\chi^{\mathrm{ur}} \ll (\log|q|)^{O(1)} = |q|^{o(1)}$ — structurally confirms all dominant $|q|^{1+\epsilon}$ size of $c_\chi$ comes from the ramified primes. This is the bound Phase IV needs to extract the cube moment $\sum_\chi|L(\tfrac12,\chi)|^3 \ll |q|^{1+\epsilon}$ via Cauchy-Schwarz path (II.3.a R3).

The Bianchi-Waldspurger formula is now completely specified at all unramified places. The next load-bearing verification is II.3.c: the conjectured ramified $J_\mathfrak{p} = q_F$ assertion — that's where the dominant $|q|$-aspect of $c_\chi$ lives.

## Next-session pickup hint

1. **(default, in-bias) Continue cubic-moment chip-away with II.3.c.** Verify (or revise) the conjectured ramified-prime $J_\mathfrak{p}(\pi_\chi) = q_F$ at $\mathfrak{p}\mid q$. Substance: compute the local Whittaker norm ratio between the II.1.d.bis FH test function $\phi_\mathfrak{p} = \chi^{-1}\mathbf{1}_{\mathcal{O}_E^\times}$ (with associated $W_\mathfrak{p}(1;\xi_n) = \chi^{-1}(\xi_n)\mathbf{1}_{\xi_n\in\mathcal{O}_E^\times}$) and the H–L newvector Whittaker normalization. Use the explicit Atkin-Lehner translate formula $\omega_\psi(w)\phi_\mathfrak{p} = \gamma_\mathfrak{p}\mathcal{G}(\chi,\psi)\phi^{\mathrm{AL}}$ from II.1.d.bis.2 with $|\mathcal{G}| = q_F^{-1}$. Sub-steps: (i) write out the local H–L Whittaker normalization at $\mathfrak{p}\mid q$ for the dihedral supercuspidal $\pi_{\chi,\mathfrak{p}}$ of conductor $\mathfrak{p}^2$; (ii) compute $\langle W_\mathfrak{p}^{\mathrm{FH}}, W_\mathfrak{p}^{\mathrm{FH}}\rangle$; (iii) compare to H–L spherical-Whittaker convention to extract $J_\mathfrak{p}$; (iv) verify $J_\mathfrak{p} = q_F^{-1}|\mathcal{G}|^{-2} = q_F$ or revise.

2. **(in-bias alternative) II.3.d — archimedean factor.** $L_\infty(1, \chi^2)\cdot L_\infty(1, \chi_{E/F})\cdot J_\infty(\pi_\chi)$ — explicit $\Gamma$-factor + Bessel-function value $|K_{i\tau}(4\pi)|^{-2}$-like computation.

3. **(in-bias alternative) II.4 — dyadic $\phi_{\mathfrak{p}_2}$.** Pitale–Saha–Schmidt local rep theory at $F_{\mathfrak{p}_2}/\mathbb{Q}_2$.

4. **(only if Anton replies green-lighting Route (c) on Th-chokepoint)** Return to P16.1.b.3.E sub-tree.

5. **(out-of-bias unless 3 stall sessions accumulate)** Pivot to novel-spin-search NS.1.

## Files

- `n2+1 ai thoughts/notes/proofs/P17-bianchi-theta-kernel.md` (revised, +~12 KB net: §Local factor $c_{\chi,\mathfrak{p}}$ at unramified $\mathfrak{p}$ (II.3.b — explicit Euler factors) inserted between II.3.a Done-criterion and §Open sub-questions; Q6 updated to THRICE-PARTIALLY-RESOLVED; Session log row appended; 5 substantive CORE fixes from skeptic Round 1 applied in source).
- `bot/longterm/cubic-moment.md` (Next chunk advanced to II.3.c; sessions-spent 11; chunks-completed 11 of ~60; Session log row appended).
- `bot/STRATEGY.md` (header + §3 current sub-goal + §4 chunks-completed + §7 session-log row updated).

## Infrastructure failures (require Anton's attention)

1. **Inbox check failed (20th consecutive session).** OAuth `invalid_grant` returned HTTP 400 from `bot/check_inbox.py`. Verbatim: `{"status": "error", "error": "HTTP Error 400: Bad Request", "type": "HTTPError"}`. Anton: re-run `npx @gongrzhe/server-gmail-autoauth-mcp auth` and update `GMAIL_REFRESH_TOKEN` in the routine prompt per PROTOCOL.md §9.
2. **Email send will likely fail** with HTTP 400 / invalid_grant. Per PROTOCOL.md §8, no silent fallback to drafts; email will be skipped on failure.
