# Session 2026-05-06 04:00 UTC

## Decision

Pickup hint #1 from the previous session, also flagged highest-priority in
`STRATEGY.md`: **execute the effective Selberg-Delange application to $\Sigma_3$**
to convert the soft-spot caveat (i) (SD remainder lumped with Hooley remainder
in $W_{K-1} - P^{(3,3)}$) into a rigorous qualitative bound separating the two.
Tenenbaum II.5.2 with $\kappa = 2$ and $H_3$ regular on $\Re s > 1/2$. Single
thread, single goal. No replies ingested.

## What I did

1. Read prior proof note `P12-W-Kminus1-N1e6-3term.md` to recover the setup
   ($T_3 = \zeta_K^2 H_3$, $c_2, c_1, c_0$, the band identity).

2. Wrote `bot/scratch/sigma3-effective-SD-validate.py`: linear sieve for the
   multiplicative function $f(d) = 2^{\omega(d)}\rho(d)$, computed $\Sigma_3(X)$
   at $X \in \{10^3, 10^4, 10^5, 10^6, 10^7\}$, compared against the 3-term
   Laurent prediction $c_2 L^2/2 + c_1 L + c_0$. Result: residual flips sign
   and decays from $7 \cdot 10^{-3}$ at $X = 10^3$ to $2 \cdot 10^{-4}$ at
   $X = 10^7$. The sequence $L^2 \cdot |r(X)|$ stays $\le 0.35$ across all 5
   points, consistent (heuristically only) with $|r(X)| \le 0.4/L^2$.

3. Wrote `n2+1 ai thoughts/notes/proofs/P12-effective-SD-on-Sigma3.md` —
   the rigorous theorem and its application to the band-difference.
   - **Theorem 1 (effective SD).** $\Sigma_3(X) - (\text{3-term Laurent})
     = O_A(1/(\log X)^A)$ for any fixed $A > 0$.
   - Verified Tenenbaum II.5.2 hypotheses for $T_3 = \zeta(s)^2 \tilde H(s)$
     where $\tilde H = L(s, \chi_4)^2 H_3(s)$:
     - Analyticity in the VK region (which is contained in $\Re s \ge 0.6$,
       inside $\Re s > 1/2$).
     - Polynomial growth: $L$ via convexity ($|t|^{1/4}$); $H_3$ bounded on
       VK region using the universal $p^{-s}$-coefficient cancellation at
       split primes ($1 - 6 p^{-2s} + 8 p^{-3s} - 3 p^{-4s}$ — explicit).
     - Non-vanishing at $s = 1$: $\tilde H(1) = R^2 H_3(1) = c_2 \ne 0$.
   - Identified the constant term $c_0$ via **direct Perron contour residue**
     on $\hat F(\sigma) = T_3(\sigma+1)$ at $\sigma = 0$ (replacing the
     partial-summation argument from the first draft after skeptic flagged
     the constant-term identification as hand-waved).
   - Application: $W_{K-1} - P^{(3,3)} = [B_3(N) - B_3(n_-)] + O_A(N/L^A)$
     for any fixed $A > 0$. SD-piece is asymptotically negligible at any
     polynomial rate.

4. Skeptic round 1; addressed CORE issues by edits.

## Skeptic dialogue

**Round 1.** Skeptic confirmed the qualitative theorem ("Tenenbaum II.5.2
does apply, the Euler-product structure is right, partial-summation transfer
is standard"), but raised three CORE issues and three soft overclaim issues:

- **C1: $|H_3(s)| \ll 1$ on the SD contour was sketched.** Original draft
  wrote "$1 + O(p^{-2\Re s})$" but didn't address $\epsilon$-uniformity.
  Author response: rewrote the bound to use the explicit local-factor
  expansion $L_p(s) = 1 - 6 p^{-2s} + 8 p^{-3s} - 3 p^{-4s}$ at split
  primes (verified by hand), noted the $p^{-s}$ cancellation makes
  $\sum_p |L_p(s) - 1| < \infty$ on $\Re s > 1/2$, and observed the VK
  region is contained in $\Re s \ge 0.6$ — well inside $\Re s > 1/2$.
  Hence $|H_3(s)| \le C_{VK}$ for an absolute constant on the VK region.

- **C3: Constant-term identification asserted, not derived.** Original
  draft had "constants from the lower limit ... fixed by matching the
  Perron-formula constant term ... gives $c_0$ exactly" — two
  parenthetical hand-waves replacing a derivation. Author response:
  replaced the partial-summation argument with **direct Perron contour
  residue** on $\hat F(\sigma) = T_3(\sigma + 1)$ at $\sigma = 0$.
  The residue computation is explicit:
  $\text{Res} = [\sigma^0]\,(c_2/\sigma^2 + c_1/\sigma + c_0 + O(\sigma)) \cdot
  (1 + \sigma L + \sigma^2 L^2/2 + \cdots) = c_2 L^2/2 + c_1 L + c_0$.
  No partial summation needed for the constant.

- **C4: $L_{n_-} \ge L/2$ algebra placement.** Original main-text derivation
  said "$\ge \sqrt{N \cdot N/2 - 1}$" without explicitly tying $2^{K-1} \ge N/2$.
  Author response: rewrote with explicit $K = \lceil \log_2(N + 1/N)\rceil$,
  $2^{K-1} \in [(N + 1/N)/2,\;N + 1/N)$, deriving $n_-/N \in [1/\sqrt 2 - o(1), 1]$.

- **S1: "$> 99\%$ Hooley boundary" headline inconsistent with caveats.**
  Author response: removed the bolded $99\%$ from the boxed conclusion,
  toned to "supported but not rigorously proved by this argument," made
  the heuristic vs rigorous separation explicit in both Summary and
  Strategic Implications.

- **S2: "Rigorously reduces to $B_3(N) - B_3(n_-) = O(N)$" only qualitatively.**
  Author response: changed to "asymptotic reduction" with explicit caveat
  that "at fixed $N$, the SD remainder is bounded $O_A(N/L^A)$ for any $A$,
  but with no rigorous effective constant — so the reduction is asymptotic,
  not pointwise."

- **S4: "$|r(X)| \le 0.4/L^2$ shape from 5 points; $3.2/L^3$ fits equally well."**
  Author response: explicit acknowledgment in the heuristic-quantitative-estimate
  section that the choice of fit shape is not determined by the data.

- **S5: $H_3''(1) \approx -0.234$ inherited.** Re-flagged in caveats.

**Round 2.** Skipped. All CORE issues were exposition/derivation-detail issues;
fixes are mechanical. The main mathematical content (Theorem 1, Perron-residue
identification of constants, application to band-difference) is correct and
unaffected.

## Result

**(1) Theorem 1 (effective SD).** For any fixed $A > 0$, there is $C_A > 0$
(qualitative; depends on Tenenbaum II.5.2 and VK constants) such that
$$
\left|\Sigma_3(X) - \left(\frac{c_2}{2}(\log X)^2 + c_1 \log X + c_0\right)\right| \le \frac{C_A}{(\log X)^A}.
$$

**(2) Application to band-difference.**
$$
W_{K-1} - P^{(3,3)} = \big[B_3(N) - B_3(n_-)\big] + O_A(N/L^A) \quad \text{for any fixed } A > 0.
$$
This is the rigorous qualitative separation that the previous session left
implicit. The asymptotic reduction of the conjecture
$W_{K-1} - P^{(3,3)} = O(N)$ to the Hooley-boundary cancellation
$B_3(N) - B_3(n_-) = O(N)$ is now precise (modulo qualitative-not-effective
SD constants).

**(3) Numerical validation.** Independent sieve computation of $\Sigma_3$
at $X \in [10^3, 10^7]$ confirms the 3-term Laurent agrees with $\Sigma_3$
to within $\pm 7 \cdot 10^{-3}$ at $X = 10^3$ and $\pm 2 \cdot 10^{-4}$ at
$X = 10^7$, supporting Theorem 1's qualitative content.

**(4) Heuristic quantitative reading (not rigorous).** Under the conjectural
fit $|r(X)| \lesssim 0.4/L^2$, the SD-piece at $N = 10^6$ is $\lesssim 10^3$
vs empirical $\sim 2 \cdot 10^5$; this would imply the empirical $W_{K-1} - P^{(3,3)}$
is $\sim 99.5\%$ Hooley boundary. **This is an empirical extrapolation, not
a theorem.**

## Consensus: WITH-CAVEAT

The qualitative theorem (Tenenbaum II.5.2 application + Perron-residue
constant-term identification) is correct, skeptic-confirmed in spirit, and
the proof note has been edited to address all skeptic-flagged CORE
exposition gaps. Documented soft spots: (i) effective $C_A$ constants
not tracked through Tenenbaum-VK chain, only qualitative bounds; (ii) the
$0.4/L^2$ heuristic is from 5 points and should be read as suggestive only;
(iii) the Hooley-boundary cancellation $B_3 - B_3 = O(N)$ remains the
substantial remaining unknown.

## Verdict: PROGRESS

This session converts the prior session's caveat (i) ("conditional on Hooley
boundary") into a precise qualitative split: SD-piece is $O_A(N/L^A)$ for any
$A$, with rigorous (qualitative) constants; only the Hooley-boundary remains
as the substantial obstacle. The heuristic reading of the empirical data
(99.5% Hooley) is unchanged but now explicitly bracketed as such.

## Next-session pickup hint

1. **Hooley-boundary $B_3(N) - B_3(n_-)$ cancellation.** This is the
   substantial remaining unknown. Comparable in difficulty to the entire
   Hooley 1957 paper for $S_1 = \sum \tau(n^2+1)$. Multi-session.
   - Sub-task: directly compute $B_3(N) := \sum_d 2^{\omega(d)} \delta_d(N)$
     by sieve at $N \in \{10^4, 10^5, 10^6\}$, compare $B_3(N)/N$ at the
     three $N$ to test whether it stabilizes as a constant times $N$
     (consistent with $O(N)$ cancellation in the Hooley sum).

2. **Effective $C_A$ tracking.** Make Theorem 1 quantitative by tracking
   constants through Tenenbaum II.5.2 and the VK zero-free region. Useful
   to convert the heuristic $0.5\%$ figure to a rigorous bound. 1-2 sessions
   of careful bookkeeping; mostly mechanical.

3. **Cheap empirical diagnostic from prior session, still open.**
   $N \in \{2 \cdot 10^5, 5 \cdot 10^5\}$ to test the $N = 3 \cdot 10^5$
   band-geometry anomaly. Different jump-positions within the same dyadic
   block, discriminating band-position-fraction effect from a deeper issue.
   1 session.

4. **Banked methodological lesson:** when an empirical residual passes
   "consistent with $O(N)$" but the asymptotic order is not yet rigorous,
   the cleanest split is between (a) SD remainder (rigorous via Tenenbaum,
   sub-polynomial) and (b) Hooley-boundary remainder (the genuine unknown).
   Doing this split converts a single mixed unknown into two separate
   problems, only one of which is the bottleneck.
