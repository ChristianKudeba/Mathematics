# Session 2026-05-08 15:50 UTC

## Decision

Empirically preview prev session's promoted highest-EV analytic step:
$h$-averaged second moment $\sum_{h=1}^H |H_h(N)|^2$ vs $\sum_{h=1}^H D_h(N)$.
Test the hypothesis that $h$-summation makes the off-diagonal contribution
$\sum_{e_1 \ne e_2} \widetilde S_h(e_1) \widetilde S_h(e_2)$ average out, so
the ratio $R(N,H) := \sum_h |H_h|^2 / \sum_h D_h \to 1$. This applies the
banked methodological lesson: 30s–5min of compute before committing a
session to the analytic version.

## What I did

1. **Wrote** `bot/scratch/Halasz-h-averaged.py`: vectorized over $h$ via numpy.
   Per-$e$: SPF factor, compute $a_p$, multiply $2\cos(2\pi h a_p/p)$ into a
   length-$H$ array. $\sim 50$s at $N=10^7$, $H=100$.

2. **Computed** $(H_h(N), D_h(N))$ for $h = 1, \ldots, H$ at six $(N, H)$ points:
   $N \in \{10^5, 10^6, 10^7\}$, $H \in \{20, 100\}$.

3. **Tabulated** $R(N, H)$:

   | $N$ \ $H$ | 20    | 100   |
   |-----------|-------|-------|
   | $10^5$    | 0.301 | 1.675 |
   | $10^6$    | 0.082 | 0.565 |
   | $10^7$    | 0.044 | 0.122 |

   At fixed $H$, $R$ decreases monotonically with $N$. At fixed $N$, $R$
   increases monotonically with $H$. *None* of the six points is close to 1.

4. **Wrote** `n2+1 ai thoughts/notes/proofs/P12-Halasz-h-averaged.md` (8 sections).

## Skeptic dialogue

**Round 1** raised five CORE issues:

- **C1.** "REFUTED" overclaimed: 3 $N$-decades cannot rule out slow approach
  to 1 with crossover beyond $10^7$.
- **C2.** $\alpha \in [0.21, 0.29]$ fit is finite-size dominated at $H=100$,
  $N=10^5$ (only $\sim 136$ sf-good $e$ per $h$).
- **C3.** "Off-diagonal systematically negative for ALL 100 $h$" extrapolates
  beyond the small-$h$ range tested; $h \in [1, 100]$ is precisely the
  Dirichlet-kernel-biased regime, not random over $h$-space.
- **C4.** "Closes another route" mischaracterizes: hypothesis was off-diagonal
  $\to 0$; actual finding is off-diagonal *negative*, which is *more useful*
  for the upstream goal $|H_h| \ll \sqrt N$. Route redirected, not closed.
- **C5.** Cauchy–Schwarz baseline missing: $|H_h|^2 \le \mathrm{count}_{\mathrm{sfg}} \cdot D_h
  \approx 0.115 N \cdot D_h$, so $R \le 0.115 N$ trivially. The hypothesis
  $R \to 1$ was never CS-forced; refuting it doesn't refute any rigorous bound.

Cosmetic: minor inconsistency between "5 minutes of compute" (§7) and
"50s at H=100" (§2); §3 row $(N, H) = (10^5, 100)$ has $R = 1.675 > 1$
which lives in the finite-size-dominated regime.

Skeptic verified correct: the script implementation (parity_h, CRT product,
Hh/Dh accumulation), the closed-form $D_h \sim C_h N$ from prev session,
the empirical numerics.

**Author response (all 5 CORE addressed in §4, §5, §7):**
- C1 → §4 retitled "No empirical evidence for $R \to 1$ in the tested range;
  trend is wrong-signed." Removed bold "REFUTED."
- C2 → §4 caveat strengthened with finite-size diagnosis explicitly stated.
  $\alpha$ fit framed as suggestive, not asymptotic.
- C3 → §5(a) now reads "negative on the tested $h$-range" with explicit
  caveat that $[1, 100]$ is small-$h$, not random; "structural anti-correlation"
  marked as hypothesis not property.
- C4 → §7 retitled "Redirects (does NOT close)" with the full diagnosis
  "off-diagonal is negative, more useful than zero off-diagonal for upstream goal."
- C5 → §4 added "Cauchy–Schwarz baseline" paragraph explicitly noting the
  hypothesis was descriptive, not corrective.

Round 2 not run; all five CORE were framing fixes addressing material the
skeptic had already verified algebraically. Underlying numerics (six $R$
values, single-$h$ ratios, script correctness) was confirmed in round 1.

## Result

**Six exact $R(N, H)$ values across the $\{10^5, 10^6, 10^7\} \times \{20, 100\}$
grid, all below the hypothesized $R \to 1$.** Decreasing in $N$, increasing
in $H$. The empirical preview rules out the simplest version of the
"$h$-orthogonality kills off-diagonal" hypothesis on the tested range.

**Strategic implication.** The $h$-averaged route is *redirected*, not
closed: off-diagonal is *negative* on $h \in [1, 100]$ (across all 100 $h$
at $N = 10^7$), which is actually *better* for $|H_h| \ll \sqrt N$ than
zero off-diagonal would be. The follow-up question is "characterize the
sign and magnitude of $\sum_{e_1 \ne e_2} \widetilde S_h(e_1) \widetilde S_h(e_2)$"
across $h$, not "average it out by $h$-summation."

**Rigor accounting unchanged.** No new rigorous bound on $|H_h|$. New
rigorous: six exact $h$-summed second moments. New heuristic / empirical:
the "$\alpha < 1/2$" suggestion (which is below threshold) and the
"off-diagonal negative on small $h$" observation.

## Consensus: WITH-CAVEAT

Skeptic accepts (after rewrites):
- Script correctness (CRT product, parity, accumulation).
- Six $R(N, H)$ values are exact at stated $(N, H)$.
- §4 framing "no evidence for $R \to 1$, trend wrong-signed".
- §5(a) framing "off-diagonal negative on tested $h$-range" with caveats.
- §7 "redirects, not closes" framing.
- Cauchy–Schwarz baseline explicitly noted.

Caveats:
1. 3 $N$-decades, 2 $H$-points; cannot discriminate $R \to 0$ vs slow $R \to 1$.
2. $h \in [1, 100]$ is small-$h$; structural inferences may not extend to
   typical large $h$.
3. The $\alpha \in [0.21, 0.29]$ fit is below threshold for asymptotic claim.
4. "Off-diagonal negative" is empirical pattern, not proven property.

## Verdict: PROGRESS

Modest-PROGRESS (empirical preview; closes one specific hypothesis;
opens a refined direction). The methodological-lesson banked from prev
sessions paid off: $\sim 70$s of total compute time saved a session
that would have set up Dirichlet-kernel orthogonality before noticing
the data doesn't fit. As prev sessions also did, this is *negative*
PROGRESS — closing a route — but it leaves the upstream picture *better*
characterized.

## Next-session pickup hint

1. **(½ session, empirical, highest-EV)**: Compute the *autocorrelation*
   $\Phi_h(d) := \frac{1}{N} \sum_{e \le N, \mathrm{sf,good}} \widetilde S_h(e) \widetilde S_h(e + d)$
   for $d = 1, 2, 3, 5, 10, 100$ at $N = 10^6$, $h \in \{1, 5, 20\}$. The
   systematic negativity of $\sum_{e_1 \ne e_2}$ should manifest in $\Phi_h(d) < 0$
   for some $d$-range. Identifies the structural anti-correlation explicitly.

2. **(½ session, empirical)**: Extend $R(N, H)$ to $N = 3 \cdot 10^7$ at
   $H \in \{20, 100\}$ to tighten the trend-vs-saturation discrimination.
   Estimated 3 minutes of compute.

3. **(½ session, empirical)**: Test "off-diagonal negative" beyond small $h$:
   compute $|H_h|^2/D_h$ at $h \in \{500, 1000, 5000, 10000\}$ at $N = 10^6$.
   If still systematically below 1, the negativity hypothesis extends.

4. **(1 session, analytic)**: Conditional on (1)/(3), set up the autocorrelation
   $\Phi_h(d)$ analytically using the CRT product formula. The pair
   $\widetilde S_h(e) \widetilde S_h(e+d)$ becomes a product of cosines with
   coupled arguments; if the cross-prime correlations are sign-determinate,
   we get an explicit-sign closed form for $\sum_{e_1 \ne e_2}$.

5. **(Banked methodological lesson, re-confirmed)**: The 30s–5min empirical
   preview before a 1-session analytic commitment continues to discriminate
   cleanly. This session ruled out a specific orthogonality mechanism that
   would have looked attractive on paper but doesn't fit the data.

## Files

- `bot/scratch/Halasz-h-averaged.py` (new): vectorized over $h$.
- `n2+1 ai thoughts/notes/proofs/P12-Halasz-h-averaged.md` (new): 8 sections.
- Builds on: `P12-Halasz-Dh-diagonal.md` ($D_h$ closed form),
  `P12-Halasz-Hh-empirical.md` (single-$h$ data).
