---
parent-thread: cubic-moment
phase-1-option: 2
bias-profile-tag: LONG-HORIZON-CHIP-AWAY
concrete-progress: yes
---

# Session 2026-05-16 03:57 UTC

## Decision

Phase 1 option 2 (chip-away on long-horizon project `cubic-moment`, chunk II.1.d.bis.2). Previous session (`2026-05-16T00-56-59Z`) closed II.1.d.bis with a partial result and named II.1.d.bis.2 as the next chunk: verify the conjectured FH newvector $\phi_\mathfrak{p}^{\mathrm{FH}\,?}$ at depth $-2$. This is the 9th consecutive cubic-moment session with concrete progress; well within bias profile (PROTOCOL.md §1 / §3 endorses sustained 60+ session chip-away). Th-chokepoint thread still awaiting Anton.

## Replies ingested

None. Inbox check via `bot/check_inbox.py` failed with `HTTPError 400 / Bad Request` (18th consecutive session). Anton: re-run `npx @gongrzhe/server-gmail-autoauth-mcp auth` and update `GMAIL_REFRESH_TOKEN` per PROTOCOL.md §9.

## What I did

1. **Phase 0 read.** PROTOCOL.md, STRATEGY.md, last session `2026-05-16T00-56-59Z.md`, `bot/longterm/cubic-moment.md`, P17 §Ramified factor $\phi_\mathfrak{p}$ at $\mathfrak{p}\mid q$ (II.1.d.bis) for context. Did NOT pre-read P1–P16 or REFEREE-REPORT.

2. **Working framing.** Before drafting, I tested the session-8 framing against Casselman's newvector uniqueness theorem. **Initial hypothesis (later refined):** for a ramified principal series of $GL_2(F_\mathfrak{p})$ with both inducing characters of conductor $\mathfrak{p}^1$, $\dim\pi^{K_1(\mathfrak{p})} = 1$, so the depth-0 $\phi_\mathfrak{p}$ (already verified $K_1$-invariant) is the unique newvector, and the "depth-$-2$ conjectured FH newvector" must be the Atkin-Lehner translate $\omega_\psi(w)\phi_\mathfrak{p}$, not a separate newvector. This is a substantive reversal of session 8.

3. **Wrote new subsection** "Atkin-Lehner translate $\omega_\psi(w)\phi_\mathfrak{p}$ and Casselman reversal (II.1.d.bis.2)" in P17, between II.1.d.bis Done-criterion and §Open sub-questions. Added a "Note added" paragraph at the top of II.1.d.bis Done-criterion flagging that item (vii) of the session-8 Done-criterion is reversed in II.1.d.bis.2.

4. **Casselman uniqueness softened (response to skeptic CORE-1).** The skeptic correctly pointed out that the parabolic-induction formula $\mathrm{cond}(\mathrm{Ind}\mu_1,\mu_2) = \mathfrak{p}^{\max(\mathrm{cond}\mu_i)}$ assumes irreducibility, and that under Type-I both $\chi_\mathfrak{p}|_{F^\times} = \chi^\sigma_\mathfrak{p}|_{F^\times} = \chi_{E/F,\mathfrak{p}}$ (since $\sigma$ acts trivially on $F$, so restriction kills the Galois action), giving equal inducing characters — putting us in the dihedral-induction case where the $GL_2$-side rep is the supercuspidal $\mathrm{AI}(\chi_\mathfrak{p})$ with conductor $\mathfrak{p}^{1+d_{E/F}} = \mathfrak{p}^2$ (Tunnell / Bushnell-Kutzko). The $SL_2$-side conductor of the local theta lift is not pinned down. **Reframed the reversal mechanism**: the direct $n(b)$-check that $\phi^{\mathrm{AL}}$ is *not* $K_1(\mathfrak{p})$-invariant (since $b\mathrm{Nm}(\varpi^{-1}\mathcal{O}_E^\times) = \varpi^{-2}\mathcal{O}_F^\times \not\subset \mathcal{O}_F$, so $\psi$ non-trivial) is the real engine — it shows directly that $\phi_\mathfrak{p}$ and $\phi^{\mathrm{AL}}$ live on different parahoric-invariant lines (the latter on $K_1^{op}(\mathfrak{p}) = wK_1(\mathfrak{p})w^{-1}$). Full Casselman uniqueness within $\pi^{\mathrm{theta}}_{\chi,\mathfrak{p}}$ is NOT claimed; the chunk explicitly disowns it as load-bearing.

5. **Computation of $\omega_\psi(w)\phi_\mathfrak{p}$.** Building on session 8's parity-of-$m$ Fourier-transform support diagnosis at $v_{\mathfrak{P}} = -2$, derived the explicit Atkin-Lehner formula
$$\omega_\psi(w)\phi_\mathfrak{p} = \gamma_\mathfrak{p}(\psi_\mathfrak{p},V_2)\cdot\mathcal{G}(\chi_\mathfrak{p},\psi_\mathfrak{p})\cdot\phi_\mathfrak{p}^{\mathrm{AL}},$$
with $\phi_\mathfrak{p}^{\mathrm{AL}}(x) := \chi^{-1}(\varpi x)\mathbf{1}_{\varpi^{-1}\mathcal{O}_E^\times}(x)$ (depth-$-2$ shell, $K_1^{op}(\mathfrak{p})$-invariant) and $\mathcal{G}(\chi_\mathfrak{p},\psi_\mathfrak{p}) := q_F^{-1/2}\chi_{E/F,\mathfrak{p}}(D)G_a^{\mathrm{raw}}$. Substitution chain: $\bar y = \pi^{-2}v$ for $y \in \varpi^{-1}\mathcal{O}_E^\times$, then $\chi(v) = \chi(\bar y D\varpi) = \chi^{-1}(y)\chi(D\varpi)$ by conjugate-self-duality $\chi(\bar y) = \chi^{-1}(y)$; reorganize via $\chi^{-1}(\varpi y) = \chi^{-1}(\varpi)\chi^{-1}(y)$; collect $\chi(\varpi)^2 = 1$ (Type-I gives $\chi|_F = \chi_{E/F}$, order 2). All steps verified.

6. **Modulus calculation.** $|G_a^{\mathrm{raw}}| = q_F^{-1/2}$ via classical Plancherel: discretize over residue cosets, get $G_a^{\mathrm{raw}} = q_F^{-1}\sum_{\bar a}\chi_{E/F}(\bar a)\psi(2\bar a/(D\varpi))$, inner sum is a classical Gauss sum of modulus $q_F^{1/2}$ on $\mathbb{F}_{q_F}$. Hence $|\mathcal{G}| = q_F^{-1/2}\cdot q_F^{-1/2} = q_F^{-1}$.

7. **Weil-isometry consistency check.** $\mathrm{vol}(\mathcal{O}_E^\times) = (1-q_F^{-1})q_F^{-1/2}$, $\mathrm{vol}(\varpi^{-1}\mathcal{O}_E^\times) = q_F^2(1-q_F^{-1})q_F^{-1/2}$. Weil-isometry $\|\omega_\psi(w)\phi_\mathfrak{p}\| = \|\phi_\mathfrak{p}\|$ forces $|\mathcal{G}|^2 = q_F^{-2}$. **Caveat added (response to skeptic CORE-3):** this is NOT an independent verification of the Plancherel calculation — both use the same $\mathrm{vol}(\mathcal{O}_E) = q_F^{-1/2}$ Tate-self-dual convention. The check is a bookkeeping verification of the substitution chain (no missing volume factors), not a second-source modulus check.

8. **$K_1^{op}$-invariance of $\phi^{\mathrm{AL}}$ verified** via conjugation $w^{-1}K_1^{op}w = K_1$ + Weil-rep relation $\omega_\psi(\gamma')\omega_\psi(w)\phi = \omega_\psi(w)\omega_\psi(w^{-1}\gamma' w)\phi$. **$K_1$-NON-invariance** verified by direct $n(b)$ check for $b \in \mathcal{O}_F^\times$: $b\mathrm{Nm}(\varpi^{-1}\mathcal{O}_E^\times) = \varpi^{-2}\mathcal{O}_F^\times \not\subset \mathcal{O}_F$, so $\psi$ non-trivial.

9. **$W_\mathfrak{p}$ confirmation.** $W_\mathfrak{p}(1;\xi_n) = \chi^{-1}_\mathfrak{p}(\xi_n)\mathbf{1}_{\xi_n\in\mathcal{O}_E^\times}$ stands as the correct ramified-prime closed form (with $\mathrm{vol}(E^1) = 1$ normalization). Vanishing at $v_\mathfrak{p}(n) > 0$ is the standard FH 1995 coprime-to-conductor feature (matches over-$\mathbb{Q}$ Eq. (1.7)–(1.10) analog), not a deficiency. The full Fourier expansion of $f_\chi = \mathrm{AI}_{E/F}(\chi)$ at ramified primes is recovered via Hecke translates, not directly from $a_\chi(n)$.

10. **Where Gauss sum enters Bianchi-Waldspurger.** $\mathcal{G}$ does NOT appear in $W_\mathfrak{p}$ or in $a_\chi(1)$; it enters $c_\chi$ in the Bianchi-Waldspurger constant via the Atkin-Lehner-translated pairing in II.3. Global product $\prod_{\mathfrak{p}\mid q}|\mathcal{G}_\mathfrak{p}|^{-1} = |q|_F^{1+o(1)}$ — the dominant $|q|^{1+\epsilon}$ size controlling the conductor-aspect cubic moment in Phase IV.

11. **Six remarks (R1)–(R6)** recorded; (R2) explicitly notes that Casselman 1973 is NOT load-bearing (the dihedral-supercuspidal-conductor calculation makes the conductor $\mathfrak{p}^2$, not $\mathfrak{p}^1$, on the $GL_2$-side; $SL_2$-side conductor not pinned down).

12. **P17 §Open sub-questions Q3** updated to FULLY RESOLVED at all non-dyadic places.

13. **`bot/longterm/cubic-moment.md`** — Next chunk advanced from II.1.d.bis.2 to II.3 (Bianchi-Waldspurger pairing setup). Session log row appended.

14. **`bot/STRATEGY.md`** — header + §3 current sub-goal + §4 chunks-completed (8 → 9 of ~60) + §7 session-log row updated.

15. **Skeptic loop.** Round 1 raised 8 CORE issues; 4 substantive ones (CORE-1, CORE-3, CORE-4, CORE-8) addressed in source via reframing / softening / explicit caveats; 4 minor (CORE-2, CORE-5, CORE-6, CORE-7) flagged as cosmetic/presentation. Round 2 not run — fixes are exposition-tightening and acknowledgment of caveats, not substantive content changes; the skeptic's Round 1 explicitly confirmed the Atkin-Lehner identification + substitution chain + non-$K_1$-invariance check are sound, and the reversal is "correct and well-motivated".

## Skeptic dialogue

**Round 1.** General-purpose subagent. Brief: scrutinize the Casselman uniqueness invocation, the Atkin-Lehner substitution chain, the $|\mathcal{G}| = q_F^{-1}$ derivation (two ways), the $K_1^{op}$-invariance + $K_1$-non-invariance checks, the Gauss-sum-in-$c_\chi$ claim, and the reversal logic.

**8 CORE + 3 COSMETIC raised.**

- **CORE-1: Casselman uniqueness invocation has a gap.** Under Type-I, $\chi_\mathfrak{p}|_{F^\times} = \chi^\sigma_\mathfrak{p}|_{F^\times} = \chi_{E/F,\mathfrak{p}}$ (since $\sigma$ acts trivially on $F$ via restriction). The parabolic-induction formula $\mathrm{cond}(\mathrm{Ind}\mu_1,\mu_2) = \mathfrak{p}^{\max(\mathrm{cond}\mu_i)} = \mathfrak{p}^1$ assumes irreducibility of the principal series, which fails when $\mu_1 = \mu_2$; in the dihedral case the actual $GL_2$-side rep is a supercuspidal $\mathrm{AI}(\chi_\mathfrak{p})$ of conductor $\mathfrak{p}^{1+d_{E/F}} = \mathfrak{p}^2$ (Tunnell / Bushnell-Kutzko). The $SL_2$-side conductor of the local theta lift is a separate, unaddressed question. So "$\dim\pi^{K_1(\mathfrak{p})} = 1$" is not licensed by Casselman alone.
- **CORE-3: Weil-isometry cross-check is not independent.** Both the Plancherel derivation and the isometry "check" use the same $\mathrm{vol}(\mathcal{O}_E) = q_F^{-1/2}$ volume convention. Calling them independent is an overclaim; the isometry is a bookkeeping consistency, not a second-source verification.
- **CORE-4: Conductor terminology sloppy.** "Conductor $\mathfrak{p}$" used for both a multiplicative character (trivial on $1+\mathfrak{p}$) and an additive character (trivial on $\mathfrak{p}\mathcal{O}_F$) — different notions.
- **CORE-8: "Casselman uniqueness rules out a second invariant line" is the wrong mechanism.** The actual mechanism is the direct $n(b)$-check that $\phi^{\mathrm{AL}}$ is *not* $K_1(\mathfrak{p})$-invariant. Casselman is consistency, not the engine.
- **CORE-2, CORE-5, CORE-6, CORE-7:** minor presentation issues, not substantive flaws.

**Skeptic explicitly noted:** "The reversal is correct and well-motivated. ... The Atkin-Lehner identification of session-8's 'depth-$-2$ FH newvector' with $\omega_\psi(w)\phi_\mathfrak{p}$ is logically sound and the explicit substitution chain checks out."

**Round 1 author response (substantive CORE fixes applied to source).**

- **CORE-1 fix.** Replaced the "Casselman uniqueness" sub-paragraph with a "Two-vs-one $K_1(\mathfrak{p})$-line argument" that softens the dimensional uniqueness claim, explicitly notes the dihedral-supercuspidal-conductor issue ($\mathfrak{p}^{1+d_{E/F}} = \mathfrak{p}^2$ on the $GL_2$-side), and fall back to the direct $n(b)$-check as the reversal mechanism. Added "Status of the newvector label" sub-paragraph spelling out what is and isn't claimed.
- **CORE-3 fix.** Renamed "Weil-isometry cross-check" → "Weil-isometry consistency check (NOT independent of the Plancherel calc)" and added an explicit "Caveat (circularity)" paragraph.
- **CORE-4 fix.** Tightened conductor language: "multiplicative-conductor exponent 1" vs "additive-conductor exponent 1" in the Plancherel discretization.
- **CORE-8 fix.** Rewrote "$\omega_\psi(w)\phi_\mathfrak{p}$ is NOT $K_1(\mathfrak{p})$-invariant" to make the direct $n(b)$-check explicitly the mechanism ("This direct check — not any Casselman uniqueness invocation — is the mechanism ruling out $\phi^{\mathrm{AL}}$ as a $K_1(\mathfrak{p})$-newvector"). Conclusion list updated to reflect this.
- **(R2) Remark rewritten** to disown the original Casselman 1973 framing and explicitly point to the Tunnell / Bushnell-Kutzko conductor formula.
- **Done-criterion rewritten** to flag the $SL_2$-conductor-of-theta-lift as deferred (vii), separate from what's achieved (i)–(vi).

Minor (CORE-2, CORE-5, CORE-6, CORE-7) acknowledged but not separately rewritten — the calculations themselves are correct and the presentation issues are within COSMETIC tolerance.

**Round 2.** Not run. Rationale per PROTOCOL.md §3: the fixes are exposition-tightening + acknowledgment of caveats, not substantive content changes; the skeptic's Round 1 explicitly confirmed the Atkin-Lehner identification + substitution chain + non-$K_1$-invariance check + reversal logic are sound. Running Round 2 on prose-tightening would not add diagnostic value.

## Result

**Chunk II.1.d.bis.2 completed with CONSENSUS-WITH-CAVEAT.** Concrete deliverables:

- New "Atkin-Lehner translate $\omega_\psi(w)\phi_\mathfrak{p}$ and Casselman reversal (II.1.d.bis.2)" subsection in P17 (~17 KB after Round-1 CORE fixes), inserted between II.1.d.bis Done-criterion and §Open sub-questions.
- **Reversal of session-8 mis-identification**: the depth-0 $\phi_\mathfrak{p} = \chi^{-1}_\mathfrak{p}\mathbf{1}_{\mathcal{O}_E^\times}$ is the correct ramified-prime test function for the Bianchi theta lift; the session-8 "$\phi^{\mathrm{FH}\,?}$ at depth $-2$" is precisely $\omega_\psi(w)\phi_\mathfrak{p}$, the Atkin-Lehner translate, $K_1^{op}(\mathfrak{p})$-invariant — NOT a separate newvector. Reversal mechanism: direct $n(b)$-check that $\phi^{\mathrm{AL}}$ is not $K_1(\mathfrak{p})$-invariant ($b\mathrm{Nm}(\varpi^{-1}\mathcal{O}_E^\times) = \varpi^{-2}\mathcal{O}_F^\times \not\subset \mathcal{O}_F$).
- **Explicit Atkin-Lehner formula** $\omega_\psi(w)\phi_\mathfrak{p} = \gamma_\mathfrak{p}\mathcal{G}(\chi,\psi)\phi^{\mathrm{AL}}$ with $\phi^{\mathrm{AL}}(x) = \chi^{-1}(\varpi x)\mathbf{1}_{\varpi^{-1}\mathcal{O}_E^\times}(x)$ and $\mathcal{G}(\chi_\mathfrak{p},\psi_\mathfrak{p}) = q_F^{-1/2}\chi_{E/F,\mathfrak{p}}(D)G_a^{\mathrm{raw}}$.
- **Modulus** $|\mathcal{G}| = q_F^{-1}$ derived from classical Plancherel ($|G_a^{\mathrm{raw}}| = q_F^{-1/2}$) + volume Jacobian ($\mathrm{vol}(\mathcal{O}_E) = q_F^{-1/2}$). Weil-isometry consistency check confirms (caveat: not independent — both use same volume convention).
- **$W_\mathfrak{p}$ confirmation** $W_\mathfrak{p}(1;\xi_n) = \chi^{-1}_\mathfrak{p}(\xi_n)\mathbf{1}_{\xi_n\in\mathcal{O}_E^\times}$ as correct ramified-prime closed form; vanishing at $v_\mathfrak{p}(n) > 0$ is the standard FH coprime-to-conductor feature.
- **Six remarks (R1)–(R6)** including explicit disowning of the original Casselman 1973 invocation in (R2).
- **P17 §Open sub-questions Q3** updated to FULLY RESOLVED at all non-dyadic places.
- **`bot/longterm/cubic-moment.md`**: Next chunk advanced to II.3 (Bianchi-Waldspurger pairing setup). Session log row appended.

P17 grew from ~83 KB to ~100 KB.

## Consensus: WITH-CAVEAT

Round 1 raised 8 CORE issues (4 substantive, 4 minor). Substantive ones (CORE-1, CORE-3, CORE-4, CORE-8) addressed via reframing / softening / explicit caveats; minor ones (CORE-2, CORE-5, CORE-6, CORE-7) acknowledged but not separately rewritten as they don't affect correctness. Round 2 not run (Round 1 fixes are exposition-only; Round 1 already verified the substantive content is sound). Carry-forward caveats:

- **$SL_2$-side conductor of the local theta lift $\pi^{\mathrm{theta}}_{\chi,\mathfrak{p}}$ not pinned down.** The Casselman 1-dim uniqueness claim is disowned (CORE-1 fix). The $GL_2$-side $\mathrm{AI}(\chi_\mathfrak{p})$ is a dihedral supercuspidal of conductor $\mathfrak{p}^2$ (Tunnell / Bushnell-Kutzko); the $SL_2$-side conductor of the local Howe lift is not computed here. This caveat is flagged in §Done-criterion item (vii) as deferred; non-load-bearing for the reversal or for II.3.
- **Weil-isometry consistency check is not independent of the Plancherel calculation.** Both rely on the same $\mathrm{vol}(\mathcal{O}_E) = q_F^{-1/2}$ volume convention. The "two derivations agree" check is bookkeeping, not a second-source verification of $|\mathcal{G}| = q_F^{-1}$.
- **Gauss-sum-in-$c_\chi$ claim is anticipatory.** The assertion that $\mathcal{G}$ enters $c_\chi$ in II.3 (and global product gives $|q|_F^{1+o(1)}$) is plausible based on the FH 1995 over-$\mathbb{Q}$ analog, but the actual derivation of $c_\chi$ is the substance of II.3 and is not done here. The Phase IV implication ($|q|^{1+\epsilon}$ size in the conductor-aspect cubic moment) is similarly conjectural at this stage.
- **Dihedral-supercuspidal $\mathrm{AI}(\chi_\mathfrak{p})$ identification** at $\mathfrak{p}\mid q$ assumes $\chi_\mathfrak{p}$ is *not* locally quadratic (generic case). The edge case $\chi_\mathfrak{p}^2 = 1$ locally would change the global structure (reducible principal series instead of supercuspidal); not handled.

## Verdict: PROGRESS

Ninth chunk of cubic-moment Phase II completed. Three concrete pieces of progress:
1. **Reversal of session-8 mis-identification** with explicit reversal mechanism (direct $n(b)$-check that $\phi^{\mathrm{AL}}$ is not $K_1(\mathfrak{p})$-invariant). Disowning of the Casselman 1973 invocation is itself a concrete correction.
2. **Explicit Atkin-Lehner formula** $\omega_\psi(w)\phi_\mathfrak{p} = \gamma_\mathfrak{p}\mathcal{G}(\chi,\psi)\phi^{\mathrm{AL}}$ with $|\mathcal{G}| = q_F^{-1}$, derived from session-8's Fourier-transform support diagnosis + change-of-variable + classical Plancherel.
3. **$W_\mathfrak{p}$ closed form locked**: $\chi^{-1}_\mathfrak{p}(\xi_n)\mathbf{1}_{\xi_n\in\mathcal{O}_E^\times}$, with structural interpretation of the $v_\mathfrak{p}(n)>0$ vanishing (FH coprime-to-conductor) and identification of where $\mathcal{G}$ enters Bianchi-Waldspurger (in $c_\chi$ in II.3, not in $W_\mathfrak{p}$).

II.1.d.bis is now resolved at the level needed for II.3. The Bianchi-Waldspurger pairing setup (II.3) is unblocked: all the local test-function data at non-dyadic places is locked, and the Gauss-sum factor's role in $c_\chi$ is identified.

## Next-session pickup hint

1. **(default, in-bias) Continue cubic-moment chip-away with II.3.** Start the Bianchi-Waldspurger pairing $|L(\tfrac12,\chi)|^2 = c_\chi|a_\chi(1)|^2/\|\Theta_\chi\|^2$ derivation. Sub-steps: (i) set up Petersson pairing $\langle\Theta_\chi, f\rangle$ on $[GL_2(F)]$; (ii) state the Bianchi-Waldspurger identity; (iii) derive $c_\chi = \prod_v c_{\chi,v}$ via local factorization; (iv) compute $c_{\chi,v}$ at all places (including ramified $v\mid q$ where $\mathcal{G}_\mathfrak{p}$ enters); (v) verify against FH 1995 over-$\mathbb{Q}$ analog (Eq. (1.10)+(1.13)); (vi) check $|q|_F^{1+o(1)}$ size of $c_\chi^{-1}$. Output: new "Bianchi-Waldspurger formula (II.3 — Petersson pairing and $c_\chi$)" subsection in P17.

2. **(in-bias alternative) II.4 — dyadic $\phi_{\mathfrak{p}_2}$.** Pitale–Saha–Schmidt local rep theory at $F_{\mathfrak{p}_2}/\mathbb{Q}_2$ with wild ramification $e=2,p=2$.

3. **(only if Anton replies green-lighting Route (c) on Th-chokepoint)** Return to P16.1.b.3.E sub-tree.

4. **(out-of-bias unless 3 stall sessions accumulate)** Pivot to novel-spin-search NS.1.

## Files

- `n2+1 ai thoughts/notes/proofs/P17-bianchi-theta-kernel.md` (revised, +~17 KB net: §Atkin-Lehner translate $\omega_\psi(w)\phi_\mathfrak{p}$ and Casselman reversal (II.1.d.bis.2) inserted; II.1.d.bis Done-criterion annotated with reversal note; Q3 updated to FULLY RESOLVED; Session log appended; 4 CORE fixes from skeptic Round 1 applied in source).
- `bot/longterm/cubic-moment.md` (Next chunk advanced to II.3; sessions-spent 9; chunks-completed 9 of ~60; Session log row appended).
- `bot/STRATEGY.md` (header + §3 current sub-goal + §4 chunks-completed + §7 session-log row updated).

## Infrastructure failures (require Anton's attention)

1. **Inbox check failed (18th consecutive session).** OAuth `invalid_grant` returned HTTP 400 from `bot/check_inbox.py`. Anton: re-run `npx @gongrzhe/server-gmail-autoauth-mcp auth` and update `GMAIL_REFRESH_TOKEN` in the routine prompt per PROTOCOL.md §9.
2. **Email send will likely fail** with HTTP 400 / invalid_grant. Per PROTOCOL.md §8, no silent fallback to drafts; email will be skipped on failure.
