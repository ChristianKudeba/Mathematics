# Session 2026-05-08 09:47 UTC

## Decision

Pickup hint #2 from prev session (2026-05-08 06:50): empirically preview the
Halász direction by computing $H_h(N) := \sum_{e \le N, \mathrm{sf, good}} \widetilde S_h(e)$
at $N \in \{10^5, 10^6, 10^7\}$, $h \in \{1, 2, 5, 100\}$, where
$\widetilde S_h(e) := \sum_{r:\, r^2 \equiv -1 \pmod e} e^{-2\pi i h r/e}$. This isolates
the cancellation contributed by the multiplicative root-weight (the binding
direction prev session identified) from the $e$-direction phase. Implements
the banked methodological lesson "preview the analytic input empirically
before committing a session to its execution."

## What I did

1. **Wrote** `bot/scratch/Halasz-Hh-empirical.py`: per-$e$ SPF sieve + CRT
   product formula
   $\widetilde S_h(e) = \prod_{p \mid e} 2 \cos(2\pi h\, \alpha_p (e/p)^{-1}_p / p)$
   ($p = 2$ contributes $(-1)^h$). $\le 30$s at $N = 10^7$.

2. **Vertical scan** ($N \in \{10^5, 10^6, 10^7\}$, $h \in \{1, 2, 5, 100\}$,
   12 points). All 12 ratios $|H_h(N)|/\sqrt N$ are bounded by 1.36, most
   well below 0.5. At $N = 10^7$, max ratio is 0.68 (at $h = 100$).

3. **Horizontal scan** ($h \in \{1,\ldots,1000\}$ across 11 values, $N = 10^6$).
   Max $|H_h|/\sqrt N$ across the scan is 1.18 (at $h = 200$).

4. **Comparison to $T_h$ at $N = 10^6$**: $|T_h| \approx 10\, |H_h|$ for
   $h \in \{1, 5\}$ — $T_h$ is *larger*, not smaller, than the unmodulated
   $H_h$. (Two data points only; treated as weak evidence.)

5. **Wrote** `n2+1 ai thoughts/notes/proofs/P12-Halasz-Hh-empirical.md`,
   9 sections, with empirical conjecture explicitly *withdrawn* in §6 in
   favor of "empirical observation only" framing.

6. **Identified next analytic step**: dyadic Abel + stationary-phase /
   van der Corput on each block $x \in [E, 2E]$, where the phase
   $e^{2\pi i h N/x}$ has stationary point governed by $h N/x^2$.
   Crude Abel is uninformative; this is the real route.

## Skeptic dialogue

**Round 1** raised four CORE issues, plus several non-core verifications:

- **C1.** §7's bound $|T_h| \ll h N (\log N)^A$ from crude Abel is *not* a
  real obstruction — it's a defect of throwing away the oscillation in
  $e^{2\pi i h N/x}$. Note framed it as if a real wall.
- **C2.** §4's "factor 250" Halász comparison is apples-to-oranges. Halász
  gives log-saving rates ($N/(\log N)^c$); $\sqrt N$ is a CLT regime, not
  a "stronger Halász." Different regimes.
- **C3.** Conjecture H over-claims uniformity in $h \le N^{o(1)}$ — only
  $h \le 1000$ tested at one $N$. Three $N$-scales is too thin for an
  envelope conjecture.
- **C4.** §5's "$e$-phase doesn't add cancellation" rests on two ratios.
  CLT magnitude alone could explain factor 10 spread.

NON-CORE verifications by skeptic (all confirmed correct): the CRT product
formula §2 + script implementation match; $p = 2$ factor $(-1)^h$ is correct
(via $(e/2)$ odd ⇒ $(e/2)^{-1}_2 = 1$); sign convention vs prev $T_h$ is
consistent; almost-multiplicativity caveat in §8 is properly flagged.

**Author response (all four CORE addressed):**
- C1 → §7(iii) rewritten: "crude Abel is uninformative"; identified
  dyadic-Abel+stationary-phase as the real route.
- C2 → §4 completely rewritten: deleted the "factor 250" framing; replaced
  with "the empirical regime is square-root, not log-saving" — square-root
  is a CLT-style rate not derivable from abstract Halász.
- C3 → §6 retitled "Empirical observation (not a conjecture)" with the
  Conjecture H statement *withdrawn* and three caveats listed explicitly.
- C4 → §5 retitled "(two data points; weak)" with the structural inference
  softened to "consistent with — but does not establish."

Round 2 not run; all four CORE were framing/promotion fixes addressing
material already in the note, not new errors. The implementation is correct;
empirical numbers are correct; only the *interpretation* needed tightening.

Cosmetic fix: script docstring claimed $H_h(N) := N^{-1} \sum$ but code
returned the unnormalized $\sum$ — docstring corrected.

## Result

**Empirical preview: the Halász-on-$\widetilde S_h$ direction is at least as
strong as predicted, possibly much stronger.** Across 23 measured data points
$(N, h)$ with $N \le 10^7$, $h \le 1000$, the ratio $|H_h(N)|/\sqrt N$ is
bounded by 1.36, and $|H_h(N)|/N \le 4.3 \cdot 10^{-3}$ throughout. The
data is consistent with an envelope $|H_h(N)| \ll \sqrt N (\log N)^c$ for
some $c \in [0, 2]$, but the grid is too coarse to pin $c$.

**Strategic implications.**
1. The prev session's promoted "Halász on $\widetilde S_h$" route is
   empirically *viable* — the input is at least as strong as the abstract
   theorem promises, possibly substantially stronger.
2. Crude Abel summation $H_h \to T_h$ is uninformative; the analytic next
   step is dyadic Abel + stationary-phase / van der Corput per block.
3. The "$e$-phase doesn't add cancellation" picture is consistent with —
   but not established by — the data.

**Rigor accounting unchanged.** No new rigorous content this session. What
changes: the "Halász on $\widetilde S_h$" route is empirically de-risked
enough to warrant the full analytic session next.

## Consensus: WITH-CAVEAT

Skeptic accepts (after rewrites):
- The CRT product formula §2 is correct; the script implements it faithfully.
- The empirical numbers are correct (sign convention, $p=2$ factor).
- The "empirical regime is square-root not log-saving" framing in revised §4.
- The withdrawal of Conjecture H in revised §6.
- The crude-Abel-is-uninformative framing in revised §7.

Caveats:
1. Only 3 $N$-scales (one decade apart). Pointwise behavior is non-monotone.
2. $h$-uniformity at large $N$ is not tested ($h \le 1000$ tested only at $N = 10^6$).
3. The "$e$-phase doesn't help" picture (§5) is provisional, two data points.
4. Halász hypothesis $A(N) \to \infty$ for our specific $f_h$ is not directly
   verified empirically — implicit but not measured.

## Verdict: PROGRESS

Modest-PROGRESS (empirical-preview, no rigor). Confirms the prev session's
strategic pivot: the "van der Corput on $U_h$" route is closed (prev session)
and the "Halász on $\widetilde S_h$" route is empirically de-risked (this
session). Methodological-lesson banked from prev session paid off: a 30s
empirical preview before a 1-session analytic commitment.

## Next-session pickup hint

1. **(1 session, analytic)**: Set up Halász-Tenenbaum (III.3) for the
   *almost*-multiplicative function $f_h(p) := 2\cos(2\pi h r_p/p)$ at split $p$.
   Key checks: (a) $|f_h(p)| \le 2$ (immediate); (b) Hooley/Weyl gives
   $\sum_{p \le X} f_h(p)/p \to 0$ uniformly in $h$; (c) handle the
   non-multiplicativity from $a_p(e) := \alpha_p (e/p)^{-1}_p$ depending on
   $e/p$ (probably via average over residues). Target: rigorous
   $|H_h(N)| \ll N \exp(-c \log\log N)$ uniform in $h$.

2. **(1 session, analytic)**: Once (1) is in hand, dyadic Abel + stationary-phase
   on $T_h(N) = \sum_e e^{2\pi i h N/e} \widetilde S_h(e)$. Per dyadic block
   $[E, 2E]$, write $T_h(N; E) = \int_E^{2E} e^{2\pi i h N/x} dH_h(x)$ and
   integrate by parts on the phase, exploiting $|H_h(x; E)| \ll \sqrt x \cdot$polylog
   from (1) (or the empirical analog).

3. **(½ session, empirical)**: Extend the $(N, h)$ grid for $H_h$:
   $N \in \{10^7, 3 \cdot 10^7\}$, $h \in \{1, 5, 50, 500, 5000\}$. Tightens
   the envelope estimate; tests $h$-uniformity into wider range.

4. **(½ session, empirical)**: Compute $A(N) := \sum_{p \le N, p \equiv 1(4)}
   (1 - \cos(2\pi h r_p/p))/p$ for $h \in \{1, 5, 100\}$, $N \in \{10^4, 10^5, 10^6\}$.
   Direct check that the Halász hypothesis "$A(N) \to \infty$" holds for our $f_h$.

5. **(Banked methodological lesson)**: A 30-second empirical preview cost
   nothing and gave a clear strategic green-light for the Halász direction.
   Continue applying this lesson — preview before commit.

## Files

- `bot/scratch/Halasz-Hh-empirical.py` (new): per-$e$ CRT product-formula sum.
- `n2+1 ai thoughts/notes/proofs/P12-Halasz-Hh-empirical.md` (new): the empirical
  preview note.
- Builds on: `P12-Uh-vs-Th-empirical.md` (prev session, route closure that
  motivated this preview); `P12-Lemma-3-4-empirical-Th.md` ($T_h$ data for
  comparison); `P12-Lemma-3-4-reduction.md` (the original Fourier reduction).
