---
parent-thread: cubic-moment
phase-1-option: 2
bias-profile-tag: LONG-HORIZON-CHIP-AWAY
concrete-progress: yes
---

# Session 2026-05-15 03:40 UTC

## Decision

Phase 1 option 2 (chip-away on long-horizon project `cubic-moment`, chunk II.1.a). Th-chokepoint thread escalated to Anton on the previous session (`2026-05-15T01-15-33Z.md`) because Route (a) (sign cancellation) is provisionally closed, Route (c) (test-function redesign $\widehat\Psi(1/2) = 0$) requires upstream test-function spec change the bot cannot make alone, and the explicit pickup hint #2 said: "(if Anton blocks on Route (c)) Pivot recommendation. ... Pivot to a different thread (long-horizon project: cubic-moment II.1.a, or novel-spin-search NS.1; alternatively invent new approach via Phase 1 option 1)." Cubic-moment is the load-bearing alternative path to Landau IV: P11 §6.7 reduces Landau IV to a Bianchi cubic moment over $\mathbb{Q}(i)$, conditional on Petrow–Young 2020 transferring to that setting. Per `bot/longterm/cubic-moment.md`, the named next chunk is II.1.a (theta kernel for the dual reductive pair). Sustained chip-away on a high-EV calculation across many sessions is in-bias per PROTOCOL.md §1; this is the *first* session on cubic-moment, so no stall risk.

Per PROTOCOL.md §4, opening a new chunk on a long-horizon project that doesn't yet have a P# file requires a new P# stub. Highest existing was P16 → created P17.

## Replies ingested

None — inbox check failed with HTTP 400 (10th consecutive session, OAuth `invalid_grant`). Anton: re-run `npx @gongrzhe/server-gmail-autoauth-mcp auth` and update `GMAIL_REFRESH_TOKEN` per PROTOCOL.md §9.

## What I did

1. **Phase 0 read.** PROTOCOL.md, STRATEGY.md, last session `2026-05-15T01-15-33Z.md` in full, `bot/longterm/cubic-moment.md`. Listed `n2+1 ai thoughts/notes/proofs/` to confirm next P# is P17.

2. **Inbox check** failed with HTTP 400 (10th consecutive). Continued per PROTOCOL.md §9.

3. **Created P17 stub** at `n2+1 ai thoughts/notes/proofs/P17-bianchi-theta-kernel.md` (~10 KB) with the full PROTOCOL.md §4 structure: ## Goal / ## Why now / ## Initial setup / ## Open sub-questions (Q1–Q5) / ## Session log. The Initial setup section is the substance of chunk II.1.a.

4. **Wrote chunk II.1.a** in P17:
    - **Notation:** $F = \mathbb{Q}(i)$, $\mathcal{O}_F = \mathbb{Z}[i]$, $\mathfrak{p}_2 = (1+i)$, adèle ring, additive characters $\psi_v$.
    - **Ambient groups:** dual reductive pair $(\widetilde{SL}_2, O(V))$ with $V = E$ ($E/F$ quadratic étale), $Q_V(x) = x\bar x$, $SO(V) = E^\times/F^\times$ (norm-1 torus).
    - **Schwartz–Bruhat space and Weil-rep generators:** explicit action of $n(b), m(a), w \in SL_2(F_v)$ on $\phi_v \in \mathcal{S}(E_v)$ — namely $\psi_v(b\, x\bar x) \phi_v(x)$, $\chi_V(a)|a|_v \phi_v(ax)$, $\gamma_v(\psi_v, V) \widehat\phi_v(x)$ respectively.
    - **Theta kernel formula:** $\Theta(g, h; \phi) = \sum_{x \in E} (\omega_\psi(g)\phi)(h^{-1}x)$.
    - **Theta lift of $\chi$:** $\Theta_\chi(g; \phi) = \int_{[O(V)]} \Theta(g, h; \phi) \chi(h)\,dh$.
    - **Two-step lift composition:** [INITIALLY WRONG; see SK-2 fix below.]
    - **Proposition (II.1.a)** stated formally.

5. **Skeptic loop Round 1.** General-purpose subagent. Brief: scrutinize Weil-rep generators (a), choice of $V$ and $O(V)$ vs $SO(V)$ (b), two-step Shimura-Waldspurger composition (c), additive-character normalization at $F = \mathbb{Q}(i)$ (d), over-claims vs YZZ §3.1 (e), and Q5 ($\mathrm{Mp}_2$ vs $\widetilde{SL}_2$) (f). Skeptic raised three CORE issues:
    - **SK-1 (parity of $\dim V$ vs metaplectic cover).** For $\dim V = 2$ even, $\omega_\psi$ descends from $\widetilde{SL}_2$ to $SL_2$ — no metaplectic correction needed when $\mathrm{disc}(V)$ is fixed. The parenthetical aside in the $m(a)$ entry was misleading.
    - **SK-2 (two-step lift narrative).** Direction I ($V_2 = E$, $\dim 2$, even, on $SL_2$) and Direction II ($V_3 = M_2^{\mathrm{tr}=0}$, $\dim 3$, odd, on $\widetilde{SL}_2$) are *separate* theta correspondences. The Bianchi Waldspurger formula needed for the cubic moment uses *only* Direction I — $f_\chi = \mathrm{AI}_{E/F}(\chi)$ on $GL_2$, and Petrow–Young's method goes through the $GL_2$ fourth moment of auto-induced forms, not through half-integral weight.
    - **SK-3 (additive character self-duality lattice).** Conductor of $\psi_v$ at finite $v$ in Tate convention is $\mathfrak{d}_v^{-1}$, not $\mathfrak{d}_v$ — typo.

6. **Author response (all CORE addressed in source).**
    - Added a **§Caveat (skeptic review)** block to P17 documenting SK-1/SK-2/SK-3 verbatim with provisional resolutions.
    - **Restated Proposition (II.1.a)** to use $SL_2$ throughout (not $\widetilde{SL}_2$) since $\dim V = 2$ is even, with explicit reference to the caveat.
    - **Updated `bot/longterm/cubic-moment.md` Phase II description** to commit to Direction I only (SK-2 fix) — Phase II.1 now says "$(SL_2, O(V_2))/\mathbb{Q}(i)$" not "$(\widetilde{SL}_2, PGL_2)$".
    - **Fixed conductor typo** ($\mathfrak{d}_v^{-1}$ in Tate convention) in the Proposition.

7. **Skeptic Round 2: not needed.** All three CORE issues were direct corrections to forward-looking narrative; the kernel formula in the Proposition is mathematically unchanged (only the host group $SL_2$ vs $\widetilde{SL}_2$ name was corrected). Per PROTOCOL.md §6, the source-file fixes resolve the issues.

## Result

**Chunk II.1.a completed.** Concrete deliverables:
- New P# stub `n2+1 ai thoughts/notes/proofs/P17-bianchi-theta-kernel.md` (~13 KB after corrections) with Proposition (II.1.a) — the theta-kernel formula $\Theta(g, h; \phi) = \sum_{x \in E} (\omega_\psi(g)\phi)(h^{-1} x)$ on $SL_2(\mathbb{A}_F) \times O(V)(\mathbb{A}_F)$ for $F = \mathbb{Q}(i)$, $V = E$ quadratic étale.
- Weil-representation generators $n(b), m(a), w$ recorded explicitly with $F$-specific normalizations.
- Three skeptic-corrected forward-looking caveats (SK-1/SK-2/SK-3) flagged for II.1.c (commit to $SL_2$ vs $\widetilde{SL}_2$ globally) and II.2.a (Fourier-coefficient computation).
- `bot/longterm/cubic-moment.md` Phase II description corrected: Direction I only, no half-integral-weight detour. Next chunk: II.1.b (verify absolute convergence + automorphy).

## Skeptic dialogue

**Round 1 (3 CORE).** SK-1 (metaplectic correction not needed for $\dim V = 2$ even); SK-2 (two-step lift narrative conflated Direction I/II); SK-3 (Tate-conductor typo $\mathfrak{d}_v^{-1}$ vs $\mathfrak{d}_v$). Author response: all three fixed in source — Proposition restated with $SL_2$, Phase II of `cubic-moment.md` restricted to Direction I, conductor typo corrected, full caveat block added to P17.

**Round 2: not needed.** All CORE issues directly resolved by source-file edits, not deferred as caveats.

## Consensus: WITH-CAVEAT

The kernel formula in Proposition (II.1.a) is correct (Direction I, $SL_2$, $V = E$ quadratic étale, Schwartz–Bruhat sum). The forward-looking narrative was reorganized: SK-2's Direction I-only restriction is a real change to the Phase II workplan that simplifies things going forward. The remaining caveats are: (i) SK-1's commitment to $SL_2$ throughout Phase II needs to be propagated to all Direction-I subsections (II.1.b, II.2, II.3); (ii) SK-3's character convention needs to be locked in II.1.c before Fourier-coefficient computations begin in II.2.a. None of these block the kernel definition itself.

## Verdict: PROGRESS

First chunk of a new long-horizon project completed cleanly. P17 stub created. Phase II workplan corrected (Direction I only). Next chunk II.1.b queued.

## Next-session pickup hint

1. **(default, in-bias) Continue cubic-moment chip-away with II.1.b.** Verify absolute convergence of $\Theta(g, h; \phi)$ for any factorizable $\phi \in \mathcal{S}(\mathbb{A}_E)$, and verify $SL_2(F) \times O(V)(F)$-automorphy. Quote YZZ Ch. 3 §3.1.3 / §3.2 (Poisson summation argument). Schwartz decay at $\infty$ + locally constant compact-support at finite places implies absolute convergence uniformly on compacta; $O(V)(F)$-automorphy via the standard Poisson identity for the Weil representation.
2. **(in-bias alternative) II.1.c — character/cocycle normalization commitment.** Lock $SL_2$ vs $\widetilde{SL}_2$ globally; lock Tate-conductor convention for $\psi_v$. This is a foundational hygiene chunk that unblocks II.2.a (Fourier expansion).
3. **(only if Anton replies green-lighting Route (c) on Th-chokepoint)** Return to P16.1.b.3.E sub-tree per the previous session's pickup hint #1.
4. **(out-of-bias unless 3 stall sessions accumulate)** Pivot to novel-spin-search NS.1.

## Files

- `n2+1 ai thoughts/notes/proofs/P17-bianchi-theta-kernel.md` (new, ~13 KB).
- `bot/longterm/cubic-moment.md` (Phase II description + Next chunk + Session log updated).
- `bot/STRATEGY.md` (header + §3 Th-chokepoint AWAITING ANTON status preserved; new cubic-moment row added; §7 session-log row added).

## Infrastructure failures (require Anton's attention)

1. **Inbox check failed (10th consecutive session).** OAuth `invalid_grant`. Anton: re-auth via `npx @gongrzhe/server-gmail-autoauth-mcp auth` and update `GMAIL_REFRESH_TOKEN`.
2. **Email send will likely fail** with HTTP 400 / invalid_grant. Per PROTOCOL.md §8, no silent fallback to drafts; email will be skipped on failure.
